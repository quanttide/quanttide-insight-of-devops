# AGENTS.md — 洞察记录规范

洞察（insight）不是执行报告。不要停留在"做了什么、结果如何"的表面，而要深入问题的本质。

## 原则

### 聚焦"为什么"，而非"怎么做"

- ❌ 记录：`release publish` 的 `collect_git_log` 用了 `from_tag..HEAD`，返回空所以报错了
- ✅ 洞察：工具假设使用者会按流程走，但 AI 不按流程走。工具需要在设计层面假设使用者会绕过它。

### 找根因，而非表象

- ❌ 记录：缺少 artifact 完整性检查，建议加一项 audit
- ✅ 洞察：根本原因是 devops-release 命令和 CLI 无法面对 AI 制造的海量不遵守流程随意跳过去的混乱。单个 audit 检查只能治标。

### 反脆弱视角

工具应当在设计层面假设使用者会绕过程序，而非假设使用者会遵守流程。每个步骤都应该能在任意状态下安全重入，并自动收敛到正确状态。

### 少用表格和代码，多用分析和追问

表格适合陈列事实，但洞察的核心是分析和追问：

- 这个现象背后是什么更深层的矛盾？
- 如果这个矛盾不解决，长期会怎样？
- 这个问题的存在是否说明了某个假设不成立？
